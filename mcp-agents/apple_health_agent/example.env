# Required — OpenAI API key used to generate synthetic HealthKit data
OPENAI_API_KEY=sk-...

# Optional — model used for synthetic data generation (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# MCP server
MCP_HOST=0.0.0.0
MCP_PORT=6331